#!/bin/bash

install_latex(){
  pacman -Q texlive-most &> /dev/null
  local installed=$?
  if [ installed==0 ]
  then
    echo 'Latex is already installed.'
  else
    sudo pacman -S texlive-most
    echo 'Successfully installed Latex!'
  fi
}

install_latex
